import styled from '@emotion/styled';
import { Link } from 'react-router-dom';
import { Button } from '@mui/material';

export const RefLinkStyled = styled(Link)`
    text-decoration: none;
    color: #f19502;
    font-weight: bold;
    display: block;
    margin-top: 2;
`;

export const RefButtonStyled = styled(Button)`
    color: black;
`;