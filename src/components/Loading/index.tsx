import React from 'react';
import LoadingIcon from './LoadingIcon';
import { LoadingStyled } from './index.style';

const Loading = () => {
    return (
        <LoadingStyled>
            <LoadingIcon />
        </LoadingStyled>
    );
};

export default Loading;
